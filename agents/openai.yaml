interface:
  display_name: "Video Auto Overlay"
  short_description: "video-use 到诊断 HUD Remotion 包装流程"
  default_prompt: "Use $video-auto-overlay to turn a prepared or raw video into word-timed Remotion overlay animations: draft a video-use packaging plan with the diagnostic HUD packaging patterns and keyword-animation-effects library, confirm the Remotion/React/GSAP prompt with the user, default to dark-diagnostic-hud, treat bundled examples as read-only style references, and run pre-Studio keyframe QA."
